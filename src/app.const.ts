/** Number of items willhaben displays per search results page. */
export const ITEMS_PER_PAGE = 30;

/** Maximum number of concurrent pages to fetch during list scraping. */
export const CONCURRENCY_LIMIT = 10;

/** Maximum number of concurrent detail pages to fetch during detail scraping. */
export const DETAIL_CONCURRENCY = 10;
